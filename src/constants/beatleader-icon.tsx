import { SOCIAL_SIZE } from ".";

export function BeatLeaderIcon() {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/assets/images/beatleader-logo.png"
      alt="BeatLeader Logo"
      width={SOCIAL_SIZE}
      height={SOCIAL_SIZE}
    />
  );
}
